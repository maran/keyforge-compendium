<div class="row">
  <div class="col">
    <h1 class="mt-2"><%= title @page.title %></h1>
    <% description @page.meta_description if @page.meta_description %>
    <%=raw Kramdown::Document.new(@page.content).to_html %>
  </div>
</div>
